# Codebase Conventions — Beacon

> Auto-generated by setup-audit on 2026-09-08.
> These conventions are specific to THIS codebase. AI agents should match
> these patterns when writing new code in this repo.

<!-- mtk-stamp
audited-against: bece471c091130f4332baec7296b48e99471bb9c
audited-at: 2026-09-08T09:59:48Z
mtk-version: 7.35.0
-->

> **Confidence legend:** `[EXTRACTED]` directly observed · `[INFERRED:n]` pattern with confidence · `[AMBIGUOUS]` split. Rule tags: `[ENFORCED]` zero exceptions · `[CONVENTION]` ≥80% · `[ASPIRATIONAL]` stated intent only. Every tagged line cites evidence.

## Naming Conventions

- [EXTRACTED] [ENFORCED] **Handlers:** `{Action}{Domain}Handler`, declared `internal sealed class ...Handler(deps) : IRequestHandler<...>` with a primary constructor — 146 of 152 handler files use the primary-constructor form, 0 are `public` (`grep -rlE 'IRequestHandler<' --include='*.cs' src | xargs grep -lE '^\s*public (sealed )?class \w+Handler' | wc -l`). Evidence: `src/Beacon.Core/Handlers/DataSources/DeleteDataSourceHandler.cs:6`.
- [EXTRACTED] [CONVENTION] **Requests / results:** `{Action}{Domain}Command` (89 records) for writes, `{Action}{Domain}Query` (65) for reads, `{Action}{Domain}Result` (98) for responses; all are `public record`s. Evidence: `src/Beacon.Core/Handlers/Projects/GetProjectDetailQuery.cs`, `src/Beacon.Core/Handlers/DataSources/DeleteDataSourceHandler.cs:15`.
- [EXTRACTED] **Controllers:** none — the REST surface is Minimal API `*Endpoints.cs` files (31) with a `Map{Area}Endpoints` extension each. Evidence: `src/Beacon.Api/Endpoints/ProjectsEndpoints.cs`.
- [INFERRED:0.73] **Services:** `{Domain}Service` behind an `I{Domain}Service` interface (81 `*Service.cs`, 45 `I*Service` interfaces); visibility is split — 24 `internal sealed class *Service` vs 9 `public (sealed) class *Service` (73% internal). Evidence: `src/Beacon.AI/Services/Knowledge/ValueGroundingService.cs:20`, `src/Beacon.Core/Services/InMemoryDatabaseManager.cs:11`.
- [EXTRACTED] [ENFORCED] **Tests:** classes `{Subject}Tests` with `[TestFixture]` (81 of 81 `*Tests.cs`, 0 `*Test.cs`); methods `{Subject}_{Scenario}_{Expectation}` (486 of 710, 68%) or `{Query}_Translates` for translation tests (38). Evidence: `src/Beacon.Tests/Unit/SqlSemanticLinterTests.cs:14`, `src/Beacon.Tests/Integration/QueryTranslationTests.cs`.
- [EXTRACTED] [ENFORCED] **Namespaces:** file-scoped, matching folder path 1:1 — 794 of 796 non-migration `.cs` files use `namespace X.Y;` and 0 use block form (`grep -rlE '^namespace [A-Za-z.]+\s*$' --include='*.cs' src | grep -v Migrations | wc -l`). Evidence: `src/Beacon.Core/Services/Validation/SqlSemanticLinter.cs:6`.
- [AMBIGUOUS] **Lambda parameters:** `CLAUDE.md` §3.1 prescribes `x`/`y`/`z`, but non-test LINQ lambdas use `x` 953 times and other single letters 749 times (`s` 111, `r` 102, `c` 98, `p` 90, `t` 64) — 54% vs 43%. Match `x` in new code, but do not "fix" existing call sites in unrelated changes. Evidence: `grep -rhoE '\.(Where|Select|OrderBy|Any|First\w*|Single\w*)\([a-z] =>' --include='*.cs' src | grep -v Tests`.
- [EXTRACTED] **Folders:** handlers under `Handlers/{Domain}/` (28 domains in Core, 3 in AI); services under `Services/{Area}/`; entities flat under `Data/Entities/` with a `Base/` subfolder. Evidence: `src/Beacon.Core/Handlers/Tasks`, `src/Beacon.Core/Data/Entities/Base`.

## File Organization

- [EXTRACTED] Layer-per-project, domain-per-folder: `Beacon.Core` holds `Abstractions`, `Adapters`, `Authentication`, `Authorization`, `Configuration`, `Data`, `DTOs`, `Exceptions`, `Handlers`, `Helpers`, `Mcp`, `Models`, `Services`, `Validators`, `Worker` (`ls src/Beacon.Core`). Evidence: `src/Beacon.Core/ServiceConfiguration.cs`.
- [EXTRACTED] A new feature adds: a handler file in `src/Beacon.Core/Handlers/{Domain}/` (or `src/Beacon.AI/Handlers/` when it needs LLM services), a route in the matching `src/Beacon.Api/Endpoints/{Area}Endpoints.cs`, and — if the schema changes — a migration in BOTH `src/Beacon.Core.PostgreSql/Data/Migrations` and `src/Beacon.Core.SqlServer/Data/Migrations`. Evidence: `src/Beacon.Api/Endpoints/GlossaryEndpoints.cs`, `src/Beacon.Core.PostgreSql/Data/Migrations/20260709120200_AddMcpEmbedding.cs`.
- [EXTRACTED] Large services are split with partial-class files named `{Service}.{Aspect}.cs` (`QueryService.Steps.cs`, `QueryService.Execution.cs`, `MigrationService.Sql.cs`, `LoginEndpoints.Sso.cs`). Evidence: `src/Beacon.Core/Services/QueryService.Steps.cs`, `src/Beacon.Api/Endpoints/LoginEndpoints.Sso.cs`.
- [EXTRACTED] Frontend: `src/Beacon.UI/web/src/{api,auth,components,lib,mocks,routes,test}`; design-system primitives in `components/beacon/`; one folder per route under `routes/`; tests co-located as `*.test.tsx`. Evidence: `src/Beacon.UI/web/src/components/beacon/PageHeader.tsx`, `src/Beacon.UI/web/src/routes/data-sources/relationships/RelationshipsPage.test.tsx`.

## DI Registration

- [EXTRACTED] [ENFORCED] Each project registers itself through one entry point: `AddBeaconServices` (Core, returns `BeaconBuilder`), `AddBeaconAI`, `AddBeaconMcp`, `AddBeaconApiServices` / `AddBeaconApiAuthorization`, `Add{Engine}Connector()` (9), `UsePostgreSql` / `UseSqlServer`. `Program.cs` calls these plus framework builders only. Evidence: `src/Beacon.SampleProject/Program.cs:104`, `src/Beacon.SampleProject/Program.cs:125`, `src/Beacon.Core/Configuration/BeaconBuilder.cs`.
- [EXTRACTED] [CONVENTION] Library registrations use `TryAdd*` (78 calls) so the host can override; plain `Add*` (24) is reserved for framework services and the configuration singleton. Transient is the default lifetime for stateless services (57 `TryAddTransient`). Evidence: `src/Beacon.AI/ServiceConfiguration.cs`, `src/Beacon.Core/ServiceConfiguration.cs:52`.
- [EXTRACTED] `BeaconConfiguration` is bound once via the `AddBeaconServices` options callback and registered as a singleton; services take it directly rather than through `IOptions<T>` (4 `IOptions` files total). Evidence: `src/Beacon.Core/Configuration/BeaconConfiguration.cs`, `src/Beacon.Core/ServiceConfiguration.cs:52`.

## Response & Error Handling

- [EXTRACTED] [CONVENTION] Throw, don't return: `InvalidOperationException` for business-rule violations (221 sites), `BeaconException` / `AiServiceException` for domain errors (111). `ApiExceptionMiddleware` on `/beacon/api` turns both into RFC 7807 400 responses, `UnauthorizedAccessException` into 403, everything else into a generic 500. Evidence: `src/Beacon.SampleProject/Middleware/ApiExceptionMiddleware.cs:50`, `src/Beacon.Core/Exceptions/BeaconException.cs`.
- [EXTRACTED] Endpoints return the handler result directly, or `Results<Ok<T>, NotFound>` when a route has a not-found branch. Evidence: `src/Beacon.Api/Endpoints/ProjectsEndpoints.cs:17`.
- [EXTRACTED] Legacy `Result<>`-shaped types remain only in `TemplateValidator`, `ParameterResolver`, `SchedulingService`, `EmbedTokenOptions`, `DefaultAuthorizationProvider`, `DatabaseAuthorizationProvider` — leave them, do not propagate. Evidence: `src/Beacon.Core/Adapters/Shared/TemplateValidator.cs:133`, `src/Beacon.Core/Authorization/Providers/DatabaseAuthorizationProvider.cs`.
- [EXTRACTED] Anonymous callers to protected `/beacon/api` routes get an RFC 7807 JSON 401 from `ApiKeyAuthMiddleware`; MCP callers get an RFC 9728 resource-metadata challenge. Evidence: `src/Beacon.Api/Authentication/ApiKeyAuthMiddleware.cs`, `src/Beacon.SampleProject/Program.cs`.

## Test Conventions

- [EXTRACTED] NUnit 4 + Moq + FluentAssertions (see `Directory.Packages.props`); `.Should()` in 77 of 87 files, `Mock<T>` in 40. Evidence: `src/Beacon.Tests/Unit/SqlGenerationPromptTests.cs`.
- [EXTRACTED] Data-layer behaviour is verified by LINQ→SQL translation: derive from `QueryTranslationTestBase` / build `NpgsqlTestContext.Create()` and assert on `ToQueryString()`; name the method `{Feature}_Translates`. Never `UseInMemoryDatabase` (0 hits, package not referenced). Evidence: `src/Beacon.Tests/Common/NpgsqlTestContext.cs`, `src/Beacon.Tests/Common/QueryTranslationTestBase.cs`.
- [EXTRACTED] Async EF code without a database: mock `DbSet<T>` backed by `TestAsyncQueryable`. Evidence: `src/Beacon.Tests/Common/TestAsyncQueryable.cs`, `src/Beacon.Tests/Unit/QueryServiceReadOnlyGateTests.cs`.
- [EXTRACTED] Unit tests that need a `BeaconContext` declare a private sealed test subclass inside the fixture (24 such `: BeaconContext` classes in `src/Beacon.Tests/Unit`) rather than sharing one. Evidence: `src/Beacon.Tests/Unit/ValueGroundingServiceTests.cs`.
- [EXTRACTED] HTTP contract tests use `BeaconWebApplicationFactory` (5 files) and require a live PostgreSQL via `BEACON_TEST_CONNECTION_STRING`. Evidence: `src/Beacon.Tests/Integration/Api/BeaconWebApplicationFactory.cs`.
- [EXTRACTED] Tests live in `src/Beacon.Tests/{Unit,Integration,Common}`, not beside the source; frontend tests are co-located `*.test.tsx` on Vitest + Testing Library + MSW (25 files). Evidence: `src/Beacon.Tests/Unit`, `src/Beacon.UI/web/src/routes/data-sources/relationships/RelationshipsPage.test.tsx`.

## Other Patterns

- [EXTRACTED] [CONVENTION] Data access: inject `IDbContextFactory<BeaconContext>` (126 files) and `await using var context = await contextFactory.CreateDbContextAsync(ct)` per unit of work; project with `.Select(x => new ...)` (149 files) rather than `.Include(` (22 files); one `SaveChangesAsync` per handler (0 handlers call it twice). Evidence: `src/Beacon.AI/Services/Knowledge/ValueGroundingService.cs:75`, `src/Beacon.Core/Handlers/QueryFolders/UpdateQueryFolderHandler.cs`.
- [EXTRACTED] Entities derive from `BaseEntity` (48) or `ArchivableBaseEntity` (18, soft delete via the global query filter); configuration is Fluent API in `BeaconContext.Configure{Domain}Entities()` partials (21), never annotations. Evidence: `src/Beacon.Core/Data/BeaconContext.cs:208`, `src/Beacon.Core/Data/Entities/Base/ArchivableBaseEntity.cs`.
- [EXTRACTED] Background work: define a Warp `IJob` record + `IJobHandler<T>` in `src/Beacon.SampleProject/Warp/Jobs/` and enqueue via `IBeaconScheduler`; never `BackgroundService`. Evidence: `src/Beacon.Core/Worker/IBeaconScheduler.cs`, `src/Beacon.SampleProject/Warp/Jobs/McpKnowledgeJobs.cs`.
- [EXTRACTED] LLM calls: inject `ILlmProvider` (resolved to `DelegatingLlmProvider`, which queues through `LlmRequestQueue`); never construct an SDK client outside `src/Beacon.AI/Services/LlmProviders`. Evidence: `src/Beacon.AI/Services/LlmProviders/DelegatingLlmProvider.cs`, `src/Beacon.AI/Services/Mcp/SqlGenerationService.cs`.
- [EXTRACTED] Raw SQL with identifiers: validate every schema/table/column through `SqlIdentifierGuard.Validate` and quote per engine (`[..]` MSSQL, backticks MySQL, `"…"` elsewhere); values are parameterized or sanitized, never interpolated. Evidence: `src/Beacon.Core/Services/SqlIdentifierGuard.cs`, `src/Beacon.Core/Services/Metadata/ColumnValueSampler.cs`.
- [EXTRACTED] Logging: `ILogger<T>` with identifiers and counts only — user SQL, question text, connection strings and tokens are never logged (see the `§1.11` comments at call sites). Evidence: `src/Beacon.AI/Services/Knowledge/ValueGroundingService.cs:136`, `src/Beacon.AI/Services/Mcp/AskSqlPipeline.cs:93`.
- [EXTRACTED] Git: lowercase, prefix-free commit subjects (50 of the last 50 non-merge commits); branches `feature/…`, `fix/…`, `docs/…`; PRs opened against `main`. Evidence: `.claude/rules/git-workflow.md`.
- [EXTRACTED] Frontend: Tailwind semantic tokens (`brand-*`, `surface`, `text`, `ok|warn|crit|info`) mapped onto CSS variables in `index.css` (119 `--` declarations); routes use `BrowserRouter basename="/"` and never `/app/` prefixes (0 hits); icons from `lucide-react`. Evidence: `src/Beacon.UI/web/tailwind.config.ts:16`, `src/Beacon.UI/web/src/App.tsx:74`.
