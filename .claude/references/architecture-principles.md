# Architecture Principles — Beacon

> Auto-generated by scan on 2026-09-08.
> Tech stack: dotnet (.NET 10, `net10.0`) with a React/Vite/TypeScript SPA shipped from a Razor Class Library.
> Based on analysis of 709 source files across 18 projects (`find src -name '*.cs' -not -path '*/bin/*' -not -path '*/obj/*' -not -path '*/Migrations/*' -not -path '*/Beacon.Tests/*' | wc -l`; `find src -name '*.csproj' -not -path '*/bin/*' | wc -l`).
> This document captures the ACTUAL architectural patterns in this codebase. Descriptive, not prescriptive — `⚠️ Inconsistency` markers flag where the code disagrees with itself or with `CLAUDE.md`. To refresh, run `/mtk-setup --audit`.

<!-- mtk-stamp
audited-against: bece471c091130f4332baec7296b48e99471bb9c
audited-at: 2026-09-08T09:59:48Z
mtk-version: 7.35.0
previous-stamp: none
sections-changed: 1. System Overview, 2. Layer Architecture, 3. Design Patterns in Use, 4. API Design, 5. Data Layer, 6. Testing Approach, 7. Infrastructure, 8. Cross-Cutting Concerns, 9. Inter-Service Communication, 10. Inconsistencies Found, Provenance
claims-delta: +96 ~0 -0
-->

> **Confidence legend:**
> `[EXTRACTED]` = directly observed in code (high trust — drift detection blocks).
> `[INFERRED:0.0–1.0]` = pattern inference with confidence score (drift detection flags).
> `[AMBIGUOUS]` = sources disagree or pattern is split (drift detection notes without verdict).
> Every tag must cite evidence (file:line, glob with hit count, or commit SHA).
>
> Rule tags on prescriptive lines: `[ENFORCED]` = zero exceptions found, reviewers refuse violations; `[CONVENTION]` = ≥80% of sampled sites, match it in new code; `[ASPIRATIONAL]` = stated intent not consistently reflected in code.

---

## 1. System Overview

- [EXTRACTED] Beacon is a text-to-SQL / data-access platform: a REST + SignalR API for a React shell, an MCP server for LLM clients, pluggable data-source connectors, and an AI layer (SQL generation, semantic search, knowledge graph, eval harness). Evidence: `src/Beacon.MCP/Tools/ProjectAskTool.cs`, `src/Beacon.AI/Services/Mcp/AskSqlPipeline.cs`, `src/Beacon.Api/Endpoints/BeaconApiEndpoints.cs`, `src/Beacon.Core/Services/Providers/IDataSourceProvider.cs`.
- [EXTRACTED] Every project targets `net10.0`; see `Directory.Build.props` and the per-project `<TargetFramework>` (18 of 18: `grep -rh '<TargetFramework' --include='*.csproj' src | sort | uniq -c`). Evidence: `Directory.Build.props`.
- [EXTRACTED] Package versions are centrally managed — every version lives in `Directory.Packages.props`; zero `.csproj` files carry an inline `Version=` (`grep -rl 'PackageReference[^>]*Version=' --include='*.csproj' src | wc -l` → 0). Evidence: `Directory.Packages.props`.
- [EXTRACTED] Single solution file in XML format, `Beacon.slnx`; no `.sln` remains (`ls *.sln` → none). Evidence: `Beacon.slnx`.
- [EXTRACTED] Key dependencies (versions: see `Directory.Packages.props`, never restated here): MediatR (in-process mediator), Microsoft.EntityFrameworkCore + Npgsql.EntityFrameworkCore.PostgreSQL + Microsoft.EntityFrameworkCore.SqlServer (dual-provider ORM), Dapper (hot paths), Moberg.Warp.* (background jobs), ModelContextProtocol.AspNetCore (MCP server), Pgvector.EntityFrameworkCore (embeddings), Microsoft.ML.OnnxRuntime (local embeddings), OpenAI / Azure.AI.OpenAI / Anthropic / AWSSDK.BedrockRuntime (LLM providers), SqlParserCS (SQL AST), EFCore.BulkExtensions, Microsoft.Data.Sqlite (cross-source joins), NUnit + Moq + FluentAssertions (tests). Evidence: `Directory.Packages.props`.
- [EXTRACTED] Frontend is React 18 + Vite + TypeScript + Tailwind CSS 3.4 with `react-router-dom`, `lucide-react`, and Vitest + Testing Library + MSW for tests (versions: see `src/Beacon.UI/web/package.json`). Evidence: `src/Beacon.UI/web/package.json`.

### Project map

| Project | Role | Evidence |
|---|---|---|
| `Beacon.Core` | Entities, abstract `BeaconContext`, 134 MediatR handler files, services, security, adapters, the `IDataSourceProvider` contract | `src/Beacon.Core/Data/BeaconContext.cs`, `src/Beacon.Core/Handlers` |
| `Beacon.Core.PostgreSql` / `Beacon.Core.SqlServer` | Concrete `PostgreSqlBeaconContext` / `SqlServerBeaconContext` + 16 migrations each | `src/Beacon.Core.PostgreSql/Data/Migrations`, `src/Beacon.Core.SqlServer/Data/Migrations` |
| `Beacon.AI` | LLM providers, SQL generation pipeline, knowledge graph, embeddings, learning + eval, 18 handler files | `src/Beacon.AI/Services/LlmProviders`, `src/Beacon.AI/Services/Mcp/AskSqlPipeline.cs` |
| `Beacon.MCP` | MCP server tools + discovery documents | `src/Beacon.MCP/Tools`, `src/Beacon.MCP/Services/QueryExecutionService.cs` |
| `Beacon.Api` | Minimal-API endpoints, auth middleware, SignalR hub | `src/Beacon.Api/Endpoints`, `src/Beacon.Api/Authentication` |
| `Beacon.Connector.{PostgreSql,SqlServer,MySql,BigQuery,Snowflake,Databricks,AzureSynapse,CloudWatch,Api}` | Data-source connectors registered via `Add{Engine}Connector()` | `src/Beacon.Connector.PostgreSql/ServiceCollectionExtensions.cs` |
| `Beacon.UI` | Razor Class Library shipping the built React SPA (`web/` → `wwwroot/`) | `src/Beacon.UI/web/src/App.tsx`, `src/Beacon.UI/Beacon.UI.csproj` |
| `Beacon.SampleProject` | Composition root / host (sample only, never shipped); owns `WarpDbContext` | `src/Beacon.SampleProject/Program.cs`, `src/Beacon.SampleProject/Warp` |
| `Beacon.Tests` | NUnit test project (87 files) | `src/Beacon.Tests/Beacon.Tests.csproj` |

## 2. Layer Architecture

- [EXTRACTED] Every project reference points toward `Beacon.Core`; no sibling references each other horizontally. `Beacon.MCP` → Core + AI is the only two-hop edge; the host references everything (`grep -o 'ProjectReference Include="[^"]*"' src/*/*.csproj`). Evidence: `src/Beacon.MCP/Beacon.MCP.csproj`, `src/Beacon.Api/Beacon.Api.csproj`, `src/Beacon.SampleProject/Beacon.SampleProject.csproj`.

```
SampleProject (host) → UI, Api, AI, MCP, Core, Core.{PostgreSql,SqlServer}, Connector.*
MCP                  → Core, AI
Api                  → Core
AI                   → Core
Connector.*          → Core
Core.{Pg,Sql}        → Core
UI                   → (none — static assets only)
Tests                → Core, Connector.Api, SampleProject
```

- [EXTRACTED] `Beacon.Api` does not reference `Beacon.MCP`; the anonymous MCP discovery allow-list therefore lives in Core (`McpDiscoveryPaths`) and is forwarded by MCP, so the auth middlewares in Api can read it without a sibling reference. Evidence: `src/Beacon.Core/Mcp/McpDiscoveryPaths.cs`, `src/Beacon.Api/Authentication/LoginFormAuthMiddleware.cs`.
- [EXTRACTED] The host is a pure composition root: `Program.cs` calls project-level registration extensions (`AddBeaconServices`, `AddBeaconAI`, `AddBeaconMcp`, `AddBeaconApiServices`, `Add{Engine}Connector`, `UsePostgreSql`) plus framework builders; product code does not live in the host beyond Warp wiring and sample auth. Evidence: `src/Beacon.SampleProject/Program.cs:104`, `src/Beacon.SampleProject/Program.cs:167`, `src/Beacon.SampleProject/Program.cs:170`, `src/Beacon.SampleProject/Program.cs:213`.
- [EXTRACTED] Cross-project DI registration lives in per-project `ServiceConfiguration.cs` (Core, AI, MCP, Api) or `ServiceCollectionExtensions.cs` (DB providers, connectors); 9 such files (`grep -rl 'IServiceCollection' --include='*.cs' src | grep -v Tests`). Evidence: `src/Beacon.Core/ServiceConfiguration.cs`, `src/Beacon.AI/ServiceConfiguration.cs`, `src/Beacon.MCP/ServiceConfiguration.cs`, `src/Beacon.Api/ServiceConfiguration.cs`, `src/Beacon.Core.PostgreSql/ServiceCollectionExtensions.cs`.

## 3. Design Patterns in Use

### 3.1 CQRS via MediatR

- [EXTRACTED] 152 handler files implement `IRequestHandler<,>` (134 in Core, 18 in AI; `grep -rl 'IRequestHandler<' --include='*.cs' src | cut -d/ -f2 | sort | uniq -c`). Evidence: `src/Beacon.Core/Handlers`, `src/Beacon.AI/Handlers`.
- [EXTRACTED] [ENFORCED] Handlers are `internal sealed class` with a primary constructor — 146 of 152 use the primary-constructor form and the remaining 6 are `internal sealed class` without constructor parameters; zero handlers are `public` (`grep -rlE 'IRequestHandler<' src | xargs grep -lE '^\s*public (sealed )?class \w+Handler'` → 0). Evidence: `src/Beacon.Core/Handlers/DataSources/DeleteDataSourceHandler.cs:6`.
- [EXTRACTED] [CONVENTION] Request records are named `{Action}{Domain}Command` (89) or `{Action}{Domain}Query` (65); result records `{Action}{Domain}Result` (98 distinct). Evidence: `src/Beacon.Core/Handlers/Projects/GetProjectDetailQuery.cs`, `grep -rhoE 'record \w+(Command|Query)\b' --include='*.cs' src`.
- [INFERRED:0.83] [CONVENTION] Handler, request and result live in one file — 126 of 152 handler files also declare the `IRequest` record (83%); the rest reuse a request declared elsewhere or declare only a handler. Evidence: `src/Beacon.Core/Handlers/DataSources/DeleteDataSourceHandler.cs`, `grep -rlE 'IRequestHandler<' src | xargs grep -lE ': IRequest(<|;)' | wc -l`.
- [EXTRACTED] Handlers are grouped by domain folder: 28 subfolders under `src/Beacon.Core/Handlers` (largest: `Tasks` 13, `AiActors` 13, `Projects` 11, `Dashboards` 11) and 3 under `src/Beacon.AI/Handlers`. Evidence: `src/Beacon.Core/Handlers/Tasks`, `src/Beacon.AI/Handlers/AdminSettings`.
- [EXTRACTED] No MediatR notifications / domain events: zero `INotification` or `IDomainEvent` usages outside tests (`grep -rlE '\bINotification\b|IDomainEvent' --include='*.cs' src | grep -v Tests | wc -l` → 0). Cross-concern communication is direct service calls. Evidence: `src/Beacon.Core/Handlers/DataSources/DeleteDataSourceHandler.cs:6`.

### 3.2 Builder-pattern DI

- [EXTRACTED] `AddBeaconServices(...)` returns a `BeaconBuilder`; connectors chain `Add{Engine}Connector()` (9 extension methods, one per connector: `grep -rhoE 'static \w+ Add\w+Connector\(' --include='*.cs' src | wc -l`) and the DB provider chains `UsePostgreSql(...)` / `UseSqlServer(...)`. Evidence: `src/Beacon.Core/Configuration/BeaconBuilder.cs`, `src/Beacon.Core/ServiceConfiguration.cs`, `src/Beacon.SampleProject/Program.cs:125`, `src/Beacon.Core.PostgreSql/ServiceCollectionExtensions.cs:16`.
- [EXTRACTED] [CONVENTION] Library projects register with `TryAdd*` so the host can override: 78 `TryAdd*` calls (57 Transient, 12 Singleton, 9 Scoped) versus 24 plain `Add*` calls across the four `ServiceConfiguration.cs` files (`grep -rhoE '\.(Try)?Add(Singleton|Scoped|Transient)<' src/Beacon.Core/ServiceConfiguration.cs src/Beacon.AI/ServiceConfiguration.cs src/Beacon.MCP/ServiceConfiguration.cs src/Beacon.Api/ServiceConfiguration.cs | sort | uniq -c`). Evidence: `src/Beacon.AI/ServiceConfiguration.cs`.
- [EXTRACTED] Lifetimes: singletons for configuration and provider managers (`BeaconConfiguration`, `LlmProviderManager`), scoped for MediatR handlers and user context, transient for stateless services (dominant form in Beacon.AI). Evidence: `src/Beacon.Core/ServiceConfiguration.cs`, `src/Beacon.AI/Services/LlmProviders/LlmProviderManager.cs`.

### 3.3 Connector / provider pattern

- [EXTRACTED] Data sources implement `IDataSourceProvider` and are resolved through `IDataSourceProviderFactory`; 6 concrete implementations exist (`grep -rlE ': IDataSourceProvider\b|, IDataSourceProvider\b' --include='*.cs' src | grep -v Tests | wc -l`). Evidence: `src/Beacon.Core/Services/Providers/IDataSourceProvider.cs`, `src/Beacon.Core/Services/Providers/DatabaseProvider.cs`.
- [INFERRED:0.8] The five SQL-engine connectors (PostgreSql, SqlServer, MySql, Snowflake, AzureSynapse) share Core's `DatabaseProvider` and contribute engine-specific metadata extraction / job repositories (each has exactly one Dapper file); the four non-SQL connectors (`ApiProvider`, `BigQueryProvider`, `CloudWatchProvider`, `DatabricksProvider`) implement their own provider class. Evidence: `src/Beacon.Connector.BigQuery/BigQueryProvider.cs`, `src/Beacon.Connector.CloudWatch/CloudWatchProvider.cs`, `src/Beacon.Core/Services/Providers/DatabaseProvider.cs`.

### 3.4 Background jobs via Moberg.Warp

- [EXTRACTED] Recurring and fire-and-forget work runs on Moberg.Warp with a dedicated `WarpDbContext` (its own `warp` schema and 1 migration), configured in the host with a 1 s polling interval, worker count `min(2×ProcessorCount, 20)`, and no `AddRetry()`. Evidence: `src/Beacon.SampleProject/Program.cs:84`, `src/Beacon.SampleProject/Program.cs:90`, `src/Beacon.SampleProject/Program.cs:91`, `src/Beacon.SampleProject/Warp/WarpDbContext.cs`.
- [EXTRACTED] Jobs are enqueued through the Core abstraction `IBeaconScheduler`, implemented in the host by `BeaconScheduler`; 9 distinct `IJobHandler<T>` handlers live in the host's `Warp/Jobs` folder (`grep -rhoE 'IJobHandler<\w+>' --include='*.cs' src | sort -u | wc -l`). Evidence: `src/Beacon.Core/Worker/IBeaconScheduler.cs`, `src/Beacon.SampleProject/Services/BeaconScheduler.cs`, `src/Beacon.SampleProject/Warp/Jobs/McpKnowledgeJobs.cs`.
- [EXTRACTED] No `BackgroundService` / `IHostedService` implementations exist outside generated Warp migration files (`grep -rl 'BackgroundService\|IHostedService' --include='*.cs' src | grep -v Tests` → only `src/Beacon.SampleProject/Warp/Migrations`). Evidence: `src/Beacon.SampleProject/Warp/Migrations/WarpDbContextModelSnapshot.cs`.

### 3.5 What the codebase deliberately does NOT use

- [EXTRACTED] No repository pattern over `BeaconContext`: the only `*Repository` classes are `JobRepository` (Dapper, connector job tables) and `GitHubRepository` (a model type). Evidence: `src/Beacon.Core/Worker/Repositories/JobRepository.cs`.
- [EXTRACTED] No FluentValidation (0 files), no AutoMapper / Mapster (0 files) — mapping is manual `.Select(...)` / object initializers; validation is inline `throw`. Evidence: `grep -rl 'AbstractValidator\|FluentValidation\|IMapper\|CreateMap\|TypeAdapterConfig' --include='*.cs' src | wc -l` → 0; `src/Beacon.Core/Handlers/Projects/CreateProjectHandler.cs`.
- [EXTRACTED] No `Microsoft.EntityFrameworkCore.InMemory` (not in `Directory.Packages.props`; `grep -rn 'UseInMemoryDatabase(' --include='*.cs' src | wc -l` → 0) and no Testcontainers. Evidence: `Directory.Packages.props`, `src/Beacon.Tests/Common/NpgsqlTestContext.cs`.
- [EXTRACTED] No Serilog / NLog — logging is `ILogger<T>` from Microsoft.Extensions.Logging in 85 non-test files. Evidence: `src/Beacon.AI/Services/Mcp/AskSqlPipeline.cs:25`.

## 4. API Design

- [EXTRACTED] REST surface is ASP.NET Core Minimal API only: 31 `*Endpoints.cs` files under `src/Beacon.Api/Endpoints`, zero controllers (`find src -name '*Controller*.cs' | grep -v Tests | wc -l` → 0). Route census: 74 `MapGet`, 62 `MapPost`, 15 `MapPut`, 14 `MapDelete` (`grep -rhoE '\.Map(Get|Post|Put|Delete|Patch)\(' --include='*.cs' src/Beacon.Api | sort | uniq -c`). Evidence: `src/Beacon.Api/Endpoints/ProjectsEndpoints.cs`.
- [EXTRACTED] [ENFORCED] Endpoints are thin adapters: every endpoint lambda takes `IMediator m, CancellationToken ct` and returns `m.Send(new {Request}(...), ct)` — 154 `.Send(` calls across the endpoint files, one per route (`grep -rho '\.Send(' --include='*.cs' src/Beacon.Api/Endpoints | wc -l`). The one-handler-one-endpoint invariant is enforced by `OpenApiContractTests`. Evidence: `src/Beacon.Api/Endpoints/ProjectsEndpoints.cs:14`, `src/Beacon.Tests/Integration/Api/OpenApiContractTests.cs`.
- [EXTRACTED] All endpoints hang off one group, `/beacon/api`, that applies `RequireAuthorization(AuthPolicyName)` and the `AntiforgeryEndpointFilter`; individual routes opt out with `AllowAnonymous()` (12 sites) or tighten to `AdminPolicyName` (16 sites) / `ExecuteScopePolicyName` (3 sites). Evidence: `src/Beacon.Api/Endpoints/BeaconApiEndpoints.cs:68`, `src/Beacon.Api/Endpoints/BeaconApiEndpoints.cs:70`.
- [EXTRACTED] Three named policies: `BeaconApi` (cookie or API key), `BeaconApiAdmin`, `BeaconApiExecute` (Execute scope — required by the MCP route). Evidence: `src/Beacon.Api/Endpoints/BeaconApiEndpoints.cs`, `src/Beacon.SampleProject/Program.cs:335`.
- [EXTRACTED] Error envelope is RFC 7807 problem details produced by `ApiExceptionMiddleware` on the `/beacon/api` prefix: `UnauthorizedAccessException` → 403, `InvalidOperationException` and `BeaconException` → 400 with the exception message, anything else → 500 with a generic message. Evidence: `src/Beacon.SampleProject/Middleware/ApiExceptionMiddleware.cs:50`, `src/Beacon.SampleProject/Middleware/ApiExceptionMiddleware.cs:52`, `src/Beacon.SampleProject/Program.cs`.
- [EXTRACTED] Typed results are used where a route has multiple outcomes (`Results<Ok<T>, NotFound>`), otherwise the handler result is returned directly. Evidence: `src/Beacon.Api/Endpoints/ProjectsEndpoints.cs:17`.
- [EXTRACTED] OpenAPI document served at `/openapi/v1.json` via `AddOpenApi` / `MapOpenApi`; the React client is regenerated from it with NSwag (`npm run codegen`). Evidence: `src/Beacon.SampleProject/Program.cs:176`, `src/Beacon.SampleProject/Program.cs:308`, `src/Beacon.UI/web/package.json`.
- [EXTRACTED] Real-time events go over one SignalR hub, `BeaconHub` at `/beacon/api/hub`, authorized with the `BeaconApi` policy. Evidence: `src/Beacon.SampleProject/Program.cs:326`, `src/Beacon.Api/Hubs/BeaconHub.cs`.
- [EXTRACTED] Authentication is layered: API-key middleware → JWT bearer middleware → `UseAuthentication` → `BeaconCookieAuthMiddleware` → `UseAuthorization` → optional `LoginFormAuthMiddleware` and `FirstRunSetupMiddleware`; OIDC is registered through `AddBeaconOidcAuthentication`. Evidence: `src/Beacon.SampleProject/Program.cs:274`, `src/Beacon.SampleProject/Program.cs:284`, `src/Beacon.SampleProject/Program.cs:285`, `src/Beacon.SampleProject/Program.cs:286`, `src/Beacon.Api/Authentication/ApiKeyAuthMiddleware.cs`.
- [EXTRACTED] No API versioning scheme exists (no `/v1/` segments, no version header handling) — `grep -rn 'ApiVersion\|/v1/' --include='*.cs' src/Beacon.Api | wc -l` → 0. Evidence: `src/Beacon.Api/Endpoints/BeaconApiEndpoints.cs:68`.

## 5. Data Layer

- [EXTRACTED] EF Core 10 with an abstract `BeaconContext : DbContext, IDataProtectionKeyContext` and two concrete subclasses, `PostgreSqlBeaconContext` and `SqlServerBeaconContext`; PostgreSQL adds `UseSnakeCaseNamingConvention()` and sets the Npgsql `SearchPath` to the configured schema. Evidence: `src/Beacon.Core/Data/BeaconContext.cs`, `src/Beacon.Core.PostgreSql/ServiceCollectionExtensions.cs:23`, `src/Beacon.Core.PostgreSql/ServiceCollectionExtensions.cs:34`.
- [EXTRACTED] [ENFORCED] Entity configuration is Fluent API only: 21 private `Configure{Domain}Entities()` partial methods in `BeaconContext`, zero `IEntityTypeConfiguration<T>` classes, zero data annotations in `src/Beacon.Core/Data/Entities` (`grep -rlE '^\s*\[(Table|Key|Column|Required|MaxLength|ForeignKey)\b' src/Beacon.Core/Data/Entities | wc -l` → 0). Evidence: `src/Beacon.Core/Data/BeaconContext.cs`.
- [EXTRACTED] [CONVENTION] Context lifecycle is `IDbContextFactory<BeaconContext>` with a short-lived context per unit of work — 126 non-test files inject the factory (`grep -rl 'IDbContextFactory<BeaconContext>' --include='*.cs' src | grep -v Tests | wc -l`). The 14 methods that take a `BeaconContext` parameter directly are private/internal helpers receiving the caller's already-created context, not DI injection. Evidence: `src/Beacon.AI/Services/Knowledge/ValueGroundingService.cs:21`, `src/Beacon.Core/Handlers/QueryFolders/UpdateQueryFolderHandler.cs:47`.
- [EXTRACTED] Entity base classes: `BaseEntity` (`Id`, `CreatedTime`) and `ArchivableBaseEntity : BaseEntity` (adds `ArchivedTime`); adoption across 68 entity files is 48 `BaseEntity`, 18 `ArchivableBaseEntity`, 2 with no base. Evidence: `src/Beacon.Core/Data/Entities/Base/BaseEntity.cs`, `src/Beacon.Core/Data/Entities/Base/ArchivableBaseEntity.cs`.
- [EXTRACTED] Soft delete is a global query filter generated for every `ArchivableBaseEntity` in `OnModelCreating`; callers archive rather than delete. Evidence: `src/Beacon.Core/Data/BeaconContext.cs:208`.
- [EXTRACTED] Dual, append-only migrations: 16 in `src/Beacon.Core.PostgreSql/Data/Migrations` and 16 in `src/Beacon.Core.SqlServer/Data/Migrations` (`ls <dir> | grep -v Designer | grep -v Snapshot | wc -l`), plus 1 Warp migration in the host. Migrations are applied at startup via `Database.Migrate()`. Evidence: `src/Beacon.Core.PostgreSql/Data/Migrations/20260709120200_AddMcpEmbedding.cs`, `src/Beacon.SampleProject/Program.cs:359`.
- [EXTRACTED] Default schema is `"beacon"` (`HasDefaultSchema("beacon")` in the model snapshots), configurable through `Beacon:Schema`. Evidence: `src/Beacon.Core.SqlServer/Data/Migrations/SqlServerBeaconContextModelSnapshot.cs`, `src/Beacon.Core/Configuration/BeaconConfiguration.cs`.
- [EXTRACTED] PostgreSQL also uses pgvector for `McpEmbedding` (a `vector(384)` column + HNSW index managed in the migration, written through `PgVectorColumnWriter` and invisible to the EF model). Evidence: `src/Beacon.Core.PostgreSql/Data/Migrations/20260709120200_AddMcpEmbedding.cs`, `src/Beacon.AI/Services/Embeddings/PgVectorColumnWriter.cs`, `src/Beacon.Core/Data/Entities/McpEmbedding.cs`.
- [EXTRACTED] Dapper sits alongside EF Core for metadata extraction, bulk/migration tooling, provider query execution and the connector job repositories — 17 non-test files (`grep -rl 'using Dapper' --include='*.cs' src | grep -v Tests | wc -l`): 9 in `src/Beacon.Core/Services`, one per SQL connector, plus `ColumnValueSampler`, `DatabaseProvider`, and `JobRepository`. Evidence: `src/Beacon.Core/Services/Metadata/ColumnValueSampler.cs`, `src/Beacon.Core/Services/Providers/DatabaseProvider.cs`, `src/Beacon.Core/Worker/Repositories/JobRepository.cs`.
- [EXTRACTED] EF raw SQL (`FromSqlRaw` / `ExecuteSqlRaw`) is rare: 5 non-test files, mainly the MCP dry-run / query tools and Core's startup `ServiceConfiguration`. Evidence: `src/Beacon.MCP/Tools/DryRunTool.cs`, `src/Beacon.Core/ServiceConfiguration.cs`.
- [EXTRACTED] Bulk writes go through `EFCore.BulkExtensions` wrapped by `BulkHelpers` / `DataTransferManager` and `MigrationService`, not `AddRange`. Evidence: `src/Beacon.Core/Helpers/BulkHelpers/DataTransferManager.cs`, `src/Beacon.Core/Services/MigrationService.cs`.
- [EXTRACTED] Cross-source query joins materialize intermediates into in-memory SQLite (`InMemoryDatabaseManager` on `Microsoft.Data.Sqlite`), never a cross-connector SQL join. Evidence: `src/Beacon.Core/Services/InMemoryDatabaseManager.cs:21`.
- [INFERRED:0.87] Read paths project with `.Select(...)` rather than `.Include(...)`: 149 non-test files use `.Select(`, 22 use `.Include(`, and 8 files contain both (candidates for the `CLAUDE.md` §5.6 "no Include beside Select" rule — see §10). Evidence: `src/Beacon.Core/Services/DataSourceService.cs`, `src/Beacon.AI/Services/Knowledge/KnowledgeGraphService.cs`.
- [EXTRACTED] Money / decimal handling: no `HasPrecision` or decimal column-type configuration exists in `BeaconContext` (`grep -rhoE 'HasPrecision\(|HasColumnType\("(decimal|numeric)' src/Beacon.Core | wc -l` → 0); Beacon stores no monetary amounts of its own. Evidence: `src/Beacon.Core/Data/BeaconContext.cs`.
- [EXTRACTED] Secrets at rest: data-source connection strings are encrypted with `EncryptionService` keyed by `Beacon:EncryptionKey` (the shipped `DefaultEncryptionKey` placeholder is rejected in Production by the host); API keys are SHA256-hashed in `ApiKeyService`; passwords go through `PasswordHasher`. Evidence: `src/Beacon.Core/Services/EncryptionService.cs`, `src/Beacon.Core/ServiceConfiguration.cs:35`, `src/Beacon.Core/Services/Security/ApiKeyService.cs`, `src/Beacon.Core/Services/Security/PasswordHasher.cs`.

## 6. Testing Approach

- [EXTRACTED] One test project, `Beacon.Tests`, on NUnit 4 + Moq + FluentAssertions (see `Directory.Packages.props`); 87 test files (70 `Unit`, 12 `Integration`, 5 `Common`) and 710 test methods. Evidence: `src/Beacon.Tests/Beacon.Tests.csproj`.
- [EXTRACTED] [CONVENTION] FluentAssertions is the assertion style — 77 of 87 files use `.Should()`, 10 use bare `Assert.` (89%). Moq is used in 40 files. Evidence: `src/Beacon.Tests/Unit/SqlSemanticLinterTests.cs`, `grep -rl '\.Should()' --include='*.cs' src/Beacon.Tests | wc -l`.
- [EXTRACTED] Query-translation tests are the primary data-layer strategy: `NpgsqlTestContext` builds a real Npgsql-provider context against a dummy connection string and tests assert on `ToQueryString()` — 8 files, 38 methods ending in `_Translates` (`grep -rhoE 'public (async Task|void) \w+_Translates\(' --include='*.cs' src/Beacon.Tests | wc -l`). Evidence: `src/Beacon.Tests/Common/NpgsqlTestContext.cs`, `src/Beacon.Tests/Common/QueryTranslationTestBase.cs`, `src/Beacon.Tests/Integration/QueryTranslationTests.cs`.
- [EXTRACTED] Async EF code without a database is exercised through `TestAsyncQueryable` doubles behind mocked `DbSet<T>`s. Evidence: `src/Beacon.Tests/Common/TestAsyncQueryable.cs`, `src/Beacon.Tests/Unit/QueryServiceReadOnlyGateTests.cs`.
- [EXTRACTED] Contract / harness tests boot the real host with `WebApplicationFactory` (`BeaconWebApplicationFactory`) — 5 files, including `OpenApiContractTests` (every MediatR handler exposed over HTTP) and `ExecuteScopeWiringTests`. These need a reachable PostgreSQL (`BEACON_TEST_CONNECTION_STRING`) and fail with a 302 redirect when the database has no users. Evidence: `src/Beacon.Tests/Integration/Api/BeaconWebApplicationFactory.cs`, `src/Beacon.Tests/Integration/Api/OpenApiContractTests.cs`.
- [EXTRACTED] [CONVENTION] Test method naming is `{Subject}_{Scenario}_{Expectation}` — 486 of 710 methods have exactly two underscores (68%), 122 have one (mostly `{Query}_Translates`), 58 have three. Evidence: `src/Beacon.Tests/Unit/SelfConsistencyVotingTests.cs`, `grep -rhoE 'public (async Task|void) \w+\(' --include='*.cs' src/Beacon.Tests`.
- [EXTRACTED] Test classes are named `{Subject}Tests` — 81 `*Tests.cs`, 0 `*Test.cs` — and carry `[TestFixture]` (81 files). Evidence: `src/Beacon.Tests/Unit/SqlSemanticLinterTests.cs:14`.
- [EXTRACTED] Frontend tests: 25 `*.test.ts(x)` files under `src/Beacon.UI/web/src` on Vitest + Testing Library + MSW. Evidence: `src/Beacon.UI/web/src/routes/data-sources/relationships/RelationshipsPage.test.tsx`, `src/Beacon.UI/web/package.json`.

## 7. Infrastructure

- [EXTRACTED] Hosting is self-hosted Kestrel; the React SPA is served at `/` from the `Beacon.UI` Razor Class Library (`MapBeaconUi`), the Warp dashboard at `/warp` behind `WarpDashboardAuthFilter`. Kestrel keep-alive and request-header timeouts and the default `HttpClient` timeout are all 5 minutes for long AI requests. Evidence: `src/Beacon.SampleProject/Program.cs:58`, `src/Beacon.SampleProject/Program.cs:59`, `src/Beacon.SampleProject/Program.cs:67`, `src/Beacon.SampleProject/Program.cs:343`.
- [EXTRACTED] No containerization and no IaC in the repo (`find . -name Dockerfile -o -name 'docker-compose*' -o -name '*.tf' -o -name '*.bicep'` → none). Evidence: `.github/workflows/w-build.yml`.
- [EXTRACTED] CI/CD is GitHub Actions: `w-build.yml` (release / manual dispatch → restore, build `--property WarningLevel=0`, test, pack, push `IsPackable` projects to NuGet as `Moberg.Beacon.*`, with pinned Node 22 / npm 11.11.0 for the SPA build) and `docs.yml` (Astro Starlight docs site to GitHub Pages). Evidence: `.github/workflows/w-build.yml`, `.github/workflows/docs.yml`, `docs/site/astro.config.mjs`.
- [EXTRACTED] Cloud SDK usage sites (not just references): AWS Bedrock in `BedrockProvider`, AWS CloudWatch Logs in `CloudWatchProvider`, Google BigQuery in `BigQueryProvider`, Azure OpenAI in `AzureOpenAiProvider`. No SQS/SNS, no Secrets Manager, no VPC/CDK code (`grep -rl 'IAmazonSQS\|IAmazonSNS\|SecretsManager' --include='*.cs' src | wc -l` → 0). Evidence: `src/Beacon.AI/Services/LlmProviders/BedrockProvider.cs`, `src/Beacon.Connector.CloudWatch/CloudWatchProvider.cs`, `src/Beacon.Connector.BigQuery/BigQueryProvider.cs`.
- [EXTRACTED] Configuration is a strongly typed `BeaconConfiguration` object bound in `AddBeaconServices` and registered as a singleton (`IOptions<T>` appears in only 4 files); secrets come from `appsettings.json` sections, user secrets or environment variables — `src/Beacon.SampleProject/appsettings.json` ships an empty `ConnectionStrings:BeaconContext`. Evidence: `src/Beacon.Core/Configuration/BeaconConfiguration.cs`, `src/Beacon.Core/ServiceConfiguration.cs:52`, `src/Beacon.SampleProject/appsettings.json`.
- [EXTRACTED] Observability is `ILogger<T>` only — no OpenTelemetry, no `ActivitySource`, no correlation-id middleware (`grep -rl 'OpenTelemetry\|ActivitySource\|CorrelationId' --include='*.cs' src | wc -l` → 0). Evidence: `src/Beacon.SampleProject/Program.cs`.
- [EXTRACTED] Local embeddings run on ONNX Runtime (`OnnxEmbeddingService`); no external embedding API is required. Evidence: `src/Beacon.AI/Services/Embeddings/OnnxEmbeddingService.cs`.

## 8. Cross-Cutting Concerns

- [EXTRACTED] [CONVENTION] Error strategy is exceptions, not a Result type: 221 `throw new InvalidOperationException` sites for business-rule violations and 111 `BeaconException` / `AiServiceException` throws for domain errors; the middleware maps both to 400. The legacy `Result<>`-shaped types survive only in `TemplateValidator`, `ParameterResolver`, `SchedulingService`, `EmbedTokenOptions` and two authorization providers (`grep -rlE '\bResult<[A-Z]|Result\.(Success|Failure)\(' --include='*.cs' src | grep -v Tests`). Evidence: `src/Beacon.Core/Exceptions/BeaconException.cs`, `src/Beacon.Core/Adapters/Shared/TemplateValidator.cs`, `src/Beacon.Core/Authorization/Providers/DatabaseAuthorizationProvider.cs`.
- [EXTRACTED] Caching is `IMemoryCache` for hot, rarely-changing reads (MCP settings, metadata, app settings; 5 files), invalidated by the corresponding write handler; no distributed cache (`grep -rl 'IDistributedCache\|HybridCache' --include='*.cs' src | wc -l` → 0). Evidence: `src/Beacon.Core/Services/McpSettingsProvider.cs`.
- [EXTRACTED] All LLM traffic funnels through `LlmRequestQueue` (bounded by `MaxConcurrentRequests`) behind `DelegatingLlmProvider`; the provider is runtime-swappable via `LlmProviderManager`, and SDK clients are instantiated only inside the four provider classes. Evidence: `src/Beacon.AI/Services/LlmProviders/LlmRequestQueue.cs`, `src/Beacon.AI/Services/LlmProviders/DelegatingLlmProvider.cs`, `src/Beacon.AI/Services/LlmProviders/LlmProviderManager.cs`, `src/Beacon.AI/Services/LlmProviders/OpenAiProvider.cs`.
- [EXTRACTED] MCP guardrails are defense-in-depth on every SQL path: regex `QueryGuardrailService` → dialect-aware `SqlReadOnlyAstValidator` (SqlParserCS, fail-closed on parse errors) → `ExecuteReadOnlyQueryAsync`, which on PostgreSQL wraps the statement in a `READ ONLY` transaction (`DatabaseProvider.SupportsDatabaseReadOnlyEnforcement`); PII detection and row limits are applied by the same guardrail service (12 files). Evidence: `src/Beacon.Core/Services/Security/QueryGuardrailService.cs`, `src/Beacon.Core/Services/Validation/SqlReadOnlyAstValidator.cs`, `src/Beacon.Core/Services/Providers/DatabaseProvider.cs:93`, `src/Beacon.MCP/Services/QueryExecutionService.cs`.
- [EXTRACTED] MCP audit and learning signals: all 8 tool classes call `McpAuditService`; the SQL-carrying tools (`ask`, `query`, `dry_run`) plus the catalog tools record through `McpSignalService` where applicable, with `dry_run` signals excluded from NL pattern mining. Evidence: `src/Beacon.MCP/Tools/ProjectAskTool.cs`, `src/Beacon.MCP/Tools/ProjectQueryTool.cs`, `src/Beacon.AI/Services/Learning/McpLearningAggregationService.cs`.
- [EXTRACTED] Grounding retrieval (glossary, golden exemplars, learned patterns, value grounding) is filtered by the caller's authorized `projectId`, never by data source alone — scoping is authorization. Evidence: `src/Beacon.AI/Services/Knowledge/KnowledgeGraphService.cs`, `src/Beacon.AI/Services/Eval/McpEvalService.cs`.
- [INFERRED:0.55] `CancellationToken` propagation: 359 non-test async methods declare a `CancellationToken` parameter and 296 do not (55%). Many of the latter are interface members, Warp job entry points and adapter helpers, but the split is wide enough that "always propagate" is not the observed rule. Evidence: `grep -rhoE 'async Task[^(]*\([^)]*CancellationToken' --include='*.cs' src | grep -v Tests | wc -l`, `src/Beacon.Core/Handlers/DataSources/DeleteDataSourceHandler.cs:9`.
- [EXTRACTED] Security headers / CSRF: the `/beacon/api` group runs `AntiforgeryEndpointFilter`; the antiforgery token cookie is deliberately `HttpOnly = false` (the SPA reads it) while the `Beacon.Auth` session cookie is `HttpOnly = true`, `SameSite = Lax`, `SecurePolicy = SameAsRequest`. A rate limiter is registered and applied globally. Evidence: `src/Beacon.Api/Endpoints/AntiforgeryEndpoints.cs:28`, `src/Beacon.Api/Authentication/AuthServiceExtensions.cs`, `src/Beacon.SampleProject/Program.cs:194`, `src/Beacon.SampleProject/Program.cs:305`.

## 9. Inter-Service Communication

- [EXTRACTED] Sync: REST (Minimal API under `/beacon/api`), SignalR (`/beacon/api/hub`, events `JobStatusChanged`, `NotificationCreated`, `ApprovalUpdated` scoped to `Clients.User`), and MCP Streamable HTTP (`/beacon/mcp`, 8 tools: `ask`, `query`, `dry_run`, `get_context`, `get_query_context`, `search`, `get_documentation`, `feedback`; anonymous discovery at `/.well-known/oauth-protected-resource` and `/.well-known/mcp/server-card.json`). Evidence: `src/Beacon.SampleProject/Program.cs:326`, `src/Beacon.SampleProject/Program.cs:335`, `src/Beacon.SampleProject/Program.cs:340`, `src/Beacon.MCP/Tools/ProjectAskTool.cs`, `src/Beacon.Core/Mcp/McpDiscoveryPaths.cs`.
- [EXTRACTED] Async: none between services — Beacon is a single deployable; internal asynchrony is Moberg.Warp jobs on PostgreSQL (see §3.4). Outbound notifications go through adapters (Slack, Teams, Mail, Jira, Webhook) in `src/Beacon.Core/Adapters`. Evidence: `src/Beacon.Core/Adapters/Slack/SlackMessageBuilder.cs`, `src/Beacon.Core/Adapters/Webhook/WebhookAdapter.cs`.
- [EXTRACTED] Outbound HTTP uses `IHttpClientFactory` (8 files) with the 5-minute default timeout configured once in the host; Refit is referenced for typed clients. Evidence: `src/Beacon.SampleProject/Program.cs:63`, `Directory.Packages.props`.
- [EXTRACTED] Contracts: the REST contract is the generated OpenAPI document consumed by NSwag codegen on the frontend; the MCP contract is the tool set declared with `McpServerTool` attributes plus the registry manifest in `deploy/registry/server.json`. No versioning of either contract exists. Evidence: `src/Beacon.MCP/Tools/ProjectQueryTool.cs`, `deploy/registry/server.json`.

## 10. Inconsistencies Found

⚠️ These are opportunities to standardize, not bugs.

- ⚠️ [AMBIGUOUS] **Lambda parameter naming.** `CLAUDE.md` §3.1 prescribes `x` (inner `y`, `z`), but across non-test LINQ lambdas `x` is used 953 times, `y` 58, `z` 1, and other single letters 749 (`s` 111, `r` 102, `c` 98, `p` 90, `t` 64) — `x` is only 54% of single-letter lambda parameters (`grep -rhoE '\.(Where|Select|OrderBy|OrderByDescending|Any|First\w*|Single\w*|Count|Sum|Max|Min|All)\([a-z] =>' --include='*.cs' src | grep -v Tests`). Evidence: `src/Beacon.AI/Services/Knowledge/KnowledgeGraphService.cs`, `src/Beacon.Core/Services/DataSourceService.cs`.
- ⚠️ [INFERRED:0.9] **`Include` beside `Select`.** 8 non-test files contain both `.Include(` and `.Select(x =>` — `DataSourceService`, `QueryService`, `QueryService.Execution`, `QueryVersionService`, `ProjectDocumentationService`, `KnowledgeGraphService`, `AiAlertGenerationService`, `AiActorService` — candidates for the `CLAUDE.md` §5.6 / §0.5 rule; each needs a per-site check since the two calls may be on different queries. Evidence: `src/Beacon.Core/Services/QueryVersionService.cs`, `src/Beacon.AI/Services/Ai/AiActor/AiActorService.cs`.
- ⚠️ [EXTRACTED] **`CLAUDE.md` names an entity base class that does not exist.** §5.5 lists `AuditableBaseEntity`, but only `BaseEntity` and `ArchivableBaseEntity` exist (`ls src/Beacon.Core/Data/Entities/Base`) and no entity derives from an auditable base. Evidence: `src/Beacon.Core/Data/Entities/Base/BaseEntity.cs`, `src/Beacon.Core/Data/Entities/Base/ArchivableBaseEntity.cs`.
- ⚠️ [EXTRACTED] **`CLAUDE.md` overstates the legacy `Result<>` footprint.** §2.9 / §9.7 cite the `Hybrid*` and `Jwt*` authentication providers, but `grep -rl 'Result<' src/Beacon.Core/Authentication | wc -l` → 0; the legacy shape actually lives in `TemplateValidator`, `ParameterResolver`, `SchedulingService`, `EmbedTokenOptions`, `DefaultAuthorizationProvider` and `DatabaseAuthorizationProvider`. Evidence: `src/Beacon.Core/Services/Shared/ParameterResolver.cs:80`, `src/Beacon.Core/Authorization/Providers/DefaultAuthorizationProvider.cs`.
- ⚠️ [EXTRACTED] **`CLAUDE.md` §2.1.1 points endpoints at the wrong project.** It says endpoints live in `src/Beacon.SampleProject/Endpoints/`; they live in `src/Beacon.Api/Endpoints` (31 files) and the host only calls `MapBeaconApi()`. Evidence: `src/Beacon.Api/Endpoints/BeaconApiEndpoints.cs`, `src/Beacon.SampleProject/Program.cs:311`.
- ⚠️ [EXTRACTED] **Migrations path in `CLAUDE.md` §5.9 is stale.** It cites `src/Beacon.Core.PostgreSql/Migrations/`; the actual folders are `src/Beacon.Core.PostgreSql/Data/Migrations` and `src/Beacon.Core.SqlServer/Data/Migrations`. Evidence: `src/Beacon.Core.PostgreSql/Data/Migrations/20260709120200_AddMcpEmbedding.cs`.
- ⚠️ [INFERRED:0.55] **`CancellationToken` propagation is a stated rule, not an observed one** — 45% of non-test async methods take no token (see §8). Evidence: `grep -rhE 'async Task[^(]*\([^)]*\)' --include='*.cs' src | grep -v Tests | grep -v CancellationToken | grep -v Migrations | wc -l`.
- ⚠️ [EXTRACTED] **Branch naming has one outlier.** Remote branches use `feature/` (7), `fix/` (6), `docs/` (3) and a single `feat/` (`git branch -r`), against `CLAUDE.md` §8.1's `feature/` form. Commit messages are consistent: 50 of the last 50 non-merge subjects are lowercase with no conventional-commit prefix. Evidence: `.claude/rules/git-workflow.md`.
- ⚠️ [EXTRACTED] **Bare `Assert.` survives in 10 test files** alongside the FluentAssertions majority (77 files). Evidence: `grep -rlE '\bAssert\.' --include='*.cs' src/Beacon.Tests`.

## Provenance

Generated by setup-audit at 2026-09-08T09:59:48Z against mtk-setup v7.35.0.

**Repomap input:**
- Symbol graph: `.claude/.mtk-cache/repomap.json` (fit: `fallback`, 0/0 symbols, 0 files)
- Fallback reason: `no-treesitter-csharp`; the repomap reported `defer-to-mcp`, and the csharp-lsp workspace load (`csharp_set_workspace`) failed, so no per-file LSP enrichment was possible.

> ⚠️ No deterministic symbol graph available (tree-sitter/LSP missing). Principles above are derived from scan recipes only — re-run with tree-sitter installed for evidence-backed output. Every numeric claim in this document carries the shell command that produced it so `verify-claims` can re-run it; file:line anchors were taken from `grep -n` output at the audited commit.
