{
  "mcpServers": {
    "beacon": {
      "type": "sse",
      "url": "http://localhost:5296/beacon/mcp/sse",
      "headers": {
        "Authorization": "Bearer ${BEACON_MCP_API_KEY}"
      }
    }
  }
}