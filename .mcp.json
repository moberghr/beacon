{
  "mcpServers": {
    "beacon": {
      "type": "http",
      "url": "http://localhost:5296/beacon/mcp",
      "headers": {
        "Authorization": "Bearer ${BEACON_MCP_API_KEY}"
      }
    }
  }
}
